# /w/display/SKILLS — START HERE

`display` is the visitor-facing organ of the surrounding `cambium` organism.
It is an independently re-enterable whole participating at host `w / expression`.
Its physical custody makes that host relation visible at `w/display/`, while the
organ's own address space begins again at `display:root`.

## current boundary

- **host organism:** `cambium`
- **outer host locus:** `w / expression`
- **host-side membrane:** `../interface.md`
- **physical custody:** `w/display/`
- **organ-local root:** this directory
- **current internal phenotype:** unsplit
- `INDEX.yaml` is therefore the empty mapping `{}`: no internal `w/x/z/y` vertex has
  yet been earned.
- there is deliberately **no** `_cambium.yaml` inside display. Creating one would
  falsely claim a closed internal CCCC split.
- host `w ⟦ display:root ⟧` is a scope transition, not host `ww`.

This organ exists because its whole is operationally separable: cambium can place it
as the complete visitor-facing encounter body while the organ preserves its own
source, intake and output boundary. Its future internal anatomy must still grow from
actual display pressure.

## living display tissue

The visitor-facing source currently resident here is:

- `content.json` — public wording and place-specific encounter copy;
- `papers.json` — display-owned public projection admitted from one acknowledged
  `/papers/_feed` event;
- `template.html` — outward document membrane;
- `style.css` — existing visitor-facing visual expression;
- `papers.css` — deliberately neutral first scaffold for the papers projection;
- `view.js` — display-local cambium rendering/camera behavior;
- `papers-view.js` — renderer for the admitted papers projection;
- `favicon.svg` — outward mark.

These are one **unsplit display body**, not eight semantic branches. File count and
implementation separation do not earn an internal CCCC split.

## first admitted `/papers/_feed` projection

The first bounded display metabolism of `/papers/_feed` has closed as living display
tissue in `papers.json`.

Source witness:
- source whole: `/papers`;
- source interface: `/papers/_feed`;
- admitted event: `papers-digestion-20260907T2301-b714c9d4`;
- source return state at admission: `REFRESH ACKNOWLEDGED`;
- admitted public scope: source-organism identity + title + papers-local root locus;
- census at this event: **48 source organisms**;
- papers-local distribution: `Genesis 6 · Continuity 18 · Governance 18 · Evolution 6`.

The projection deliberately excludes private Drive URLs, feed credentials, source
text, carrier/PDF duplicates, and nested-organ interiors. `papers:w/x/z/y` remain
**papers' address space** and are not cambium host `w/x/z/y` destinations.

`papers.json` is display-owned living consequence, not a mirror/authority replacing
`/papers/_feed`. The source feed remains private and source-owned. This first
projection is an admitted acknowledged **snapshot/event**, not an installed automatic
refresh path. A future refresh must preserve the explicit chain:

`papers organic return → papers/_feed refresh + acknowledgement → transport/admission
→ display metabolism → papers.json mutation → cambium build/witness`

Do not let mechanical publication fetch the private Sheet directly or make feed
arrival equal assimilation.

## host interfaces

Display does not swallow the surrounding organism's physiology merely because its
membrane uses it.

- `../../x/continuity.md` is the host **continuity** interface: visible content stays
  owned here while the host preserves the inheritance/meaning relation through
  display changes.
- `../../z/address.js`, `../../z/navigation.js`, `../../z/app.js` are host
  **orientation** interfaces. They give the outward membrane lawful address and
  navigation behavior.
- `../../y/build.py`, `../../y/check.py`, `../../y/test-address.cjs` and optional
  `../../y/browser-check.py` are host **renewal/witness** interfaces. They build and
  challenge the membrane without becoming display's inner descendants.
- `../../.github/workflows/pages.yml` is substrate circulation: a mechanical GitHub
  Actions pump that builds/tests the membrane and, only in
  `self-similar-systems/cambium`, hands the artifact to GitHub Pages.

The deployed `_site` artifact is an **ephemeral outward membrane**, not living organ
tissue and not canonical anatomy. It is generated and never committed.

## metabolism

`_stomach` contains unresolved visitor-facing nutrients routed specifically to this
organ. Current meals include:

- `INCOMING — cambium becoming.md` — the becoming narrative;
- `observations.md` — encounter/navigation pressure from actual use.

Neither meal has been digested merely by moving across the organ membrane. The first
papers feed projection is separate living consequence and does not silently digest
these pre-existing meals.

`_waste` is display-local terminal outflow. A future export may feed another
organism only through an explicit ejection/admission relation; folder identity does
not transfer metabolic role.

## re-entry

1. enter the surrounding organism through the canonical Drive `/SKILLS` receptor and
   `cambium/SKILLS/START_HERE.md`;
2. descend to host `w / expression` and read `../interface.md` when the host↔organ
   relation matters;
3. cross the explicit `w / expression ⟦ display ⟧` boundary;
4. read this receptor and `INDEX.yaml` completely;
5. because display is currently unsplit, do **not** seek or fabricate an internal
   `_cambium.yaml`;
6. read only the living display tissue or stomach nutrient required by the task;
7. when work concerns papers presentation, treat `papers.json` as current display
   tissue and `/papers/_feed` as its source interface; reacquire the source feed only
   when freshness/admission itself is part of the task.

Compression: **display owns what the visitor encounters; source feeds remain owned by
their source organisms; admitted projections become display tissue only through
bounded metabolism; cambium carries continuity, orientation and renewal across the
host membrane; the Pages artifact is only the secreted membrane.**
