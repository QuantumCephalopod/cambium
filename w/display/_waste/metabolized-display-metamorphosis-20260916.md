# INCOMING — display metamorphosis

## Pressure

Display is presently an unsplit cocoon. The site-holon primitive is now coherent enough that the remaining flat tissue should metamorphose into repository anatomy that is isomorphic to the displayed global site-space.

This nutrient is pressure only. It does **not** declare the metamorphosis complete and does **not** authorize `_feed` directly. The intended return path is:

`_stomach -> metabolize/actualize -> durable HOME in _root -> asynchronous _feed refresh -> conditional upward propagation`

## Already closed before metamorphosis

- one persistent Display membrane/runtime;
- one global infinitely recursive tetrahedral address law;
- Philosophy globally mounted at site-space overview `ε`;
- Papers globally mounted at site-space `y / Inquiry`;
- global navigator exposes mounted page-organism encounters only;
- global navigator click performs direct encounter movement;
- Philosophy alone may inspect realized global geometry without moving the encounter;
- every page-organism keeps an independently rooted local world;
- shader/background belong to interlocutor identity, not address;
- global orientation is shared across visible local fields;
- page-organism identity survives remounting;
- exact raw-address pile-up is not lawful pressure relief; differentiation must occur first;
- distinct raw addresses may still quotient-coalesce into one locus and compose as a shared encounter.

## Metamorphosis pressure

### 1. Physical repository anatomy becomes isomorphic to global site-space

`display/y` is the membrane/root of the global site-holon address-space.

Crossing into that vertex restarts the site-space address law:

`display:y ⟦ site-space:ε ⟧`

Therefore physical custody and semantic site-space relate as:

- `w/display/y/` -> site-space `ε` / overview;
- `w/display/y/yy/` -> site-space `y`;
- `w/display/y/yz/` -> site-space `z`;
- `w/display/y/yw/` -> site-space `w`;
- `w/display/y/yx/` -> site-space `x`;
- deeper recursion continues by the same tetrahedral law.

The outer Display `y` prefix is membrane custody, not part of the restarted site-space address.

### 2. Site-holons become singular autonomous bodies

Philosophy should be physically carried as one site-holon at site-space overview:

`w/display/y/philosophy/`

Papers should be physically carried as one site-holon at site-space `y / Inquiry`:

`w/display/y/yy/papers/`

A page-organism body carries its own relevant tissue with it, including as appropriate:

- stable identity declaration;
- local recursive root / INDEX;
- local RITUALS;
- shader identity and background embodiment;
- manifestation/render tissue;
- local interaction specialties;
- public projection boundary/data receptor;
- activity/live receptor;
- organism shell (`_feed`, `_root`, `_stomach`, `_waste`) where earned.

Global host placement is environment, not identity.

### 3. Tree presence becomes mount truth

A global site address exists for navigation only when a site-holon is actually present at that lawful address folder.

No empty address folders are fabricated merely to complete symmetry.

No site-holon at an address -> no global navigation target at that address.

The repository tree therefore becomes the canonical mount surface. A manually maintained flat mount table may survive only as a derived build/cache artifact if still useful; it must cease being independent semantic authority.

### 4. Raw-address occupancy and quotient coalescence remain distinct

One exact raw site-space address may not accumulate multiple direct site-holons.

If new pressure would place another site directly at an occupied raw address, that address must first differentiate through the next 1->4 tetrahedral split and affected sites must be remounted into distinct child addresses.

Self-continuation preserves place where lawful, e.g. `point(x) = point(xx)`.

Distinct raw addresses may still quotient-coalesce to one locus. If two independently addressed site-holons coalesce geometrically, the existing shared-locus split/grid encounter physiology remains lawful.

### 5. Flat cocoon tissue loses authority after successful migration

Current flat page-specific tissue under `w/display/` is transitional cocoon material. After the new addressed site-holon bodies are proven viable, superseded flat carriers should be metabolized into `_waste` or otherwise retired from canonical build authority rather than retained as competing truths.

Do not delete/move tissue merely for cosmetic cleanliness before the new bodies are browser-witnessed.

## Display 1->4 differentiation

This pressure strongly suggests that Display has earned a local tetrahedral split, with `y` already clearly differentiated as the habitat/address-space of site-holons.

Do **not** invent Display `w/x/z` phenotypes to satisfy symmetry. Re-enter through live Display RITUALS and derive the remaining three loci from existing differentiated physiology. Only actualize `_cambium.yaml` / four child loci when the complete 4V closes coherently.

## Acceptance witnesses

The metamorphosis is not closed until browser/build contact proves at least:

1. Philosophy is discovered from its physical site-holon body at `w/display/y/philosophy/` and appears at global overview `ε`.
2. Papers is discovered from `w/display/y/yy/papers/` and appears at global site-space `y / Inquiry`.
3. Global navigator targets are derived from physically present addressed site-holons, not a separately authored list.
4. Removing/moving a lawful site-holon address changes the global navigation target deterministically on rebuild.
5. **Killer relocation witness:** moving the entire `papers/` body from one lawful address folder to another requires zero changes inside Papers; rebuilding alone remounts the same identity, shader, manifestation, state/activity target and local root at the new global address.
6. No empty address folder creates a global navigation target.
7. Exact raw-address double occupation is rejected / forces differentiation before admission.
8. Distinct raw addresses that quotient-coalesce still produce a shared-locus encounter.
9. Philosophy background inspection remains Philosophy-local and continues to visualize realized global geometry independent of which global addresses currently carry sites.
10. The global Display membrane/navigation/transition physiology remains invariant across the migration.
11. Superseded flat cocoon tissue is no longer build authority after migration.
12. Only after actualized anatomy is witnessed is a durable Display HOME written to `_root`; `_feed/current.json` then refreshes mechanically from that HOME.

## Compression

**Metamorphose Display from a flat cocoon into an isomorphic recursive organism: `display/y` restarts the global site-space at `ε`; autonomous site-holons live inside lawful recursive address folders; physical presence determines global navigation; identity survives relocation; no empty geometry is fabricated; and the successful mutation returns through durable HOME -> local feed rather than being asserted directly into `_feed`.**
