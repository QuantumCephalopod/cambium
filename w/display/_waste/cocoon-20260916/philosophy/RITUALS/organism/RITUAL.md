---
name: philosophy
description: "Local ritual receptor for the first public Philosophy organism: preserve its Inquiry / Continuity / Care / Becoming constitution, translation-independent semantic identity, recursive addressability and explicit root relation to Display."
version: "1.0"
---

# PHILOSOPHY ORGANISM RITUAL — local root

`philosophy` is the first independently re-enterable semantic whole encountered through the Display organ.

## entry and boundary

Enter shared root `RITUALS/`, then cambium root `RITUALS/`, then Display `RITUALS/`, then cross `display:root ⟦ philosophy:root ⟧` through `../philosophy.interface.md`.

Physical custody is `w/display/philosophy/`; this path is a carrier relation, not a concatenated semantic address.

## current phenotype

Root `INDEX.yaml` is closed as:
- `w · Inquiry`
- `x · Continuity`
- `z · Care`
- `y · Becoming`

Root `_cambium.yaml` contains the complete current `4V / 6E / 4F / 1T` closure. Each child is currently an unsplit living locus carrying its public philosophy tissue in local `content.json`.

The split was earned from actual first-contact / care / mission pressure rather than a website taxonomy. Future refinement repeats the same inherited `CREATE / COPY / CONTROL / CULTIVATE` questions locally and materializes only when a complete split closes.

## public encounter invariant

The Philosophy root is the first full specimen of the Display page grammar:

`address → CCCC aperture → local semantic concept → encounter question`

At root:
- `w → CREATE → Inquiry → Was daran möchten wir erforschen?`
- `x → COPY → Continuity → Was sind Self-Similar Systems?`
- `z → CONTROL → Care → Warum möchten wir daran forschen?`
- `y → CULTIVATE → Becoming → Was haben wir bereits erforscht?`

Those four lateral descriptions identify the same four loci; they are not sixteen semantic nodes.

Every realized descendant must remain directly encounterable by the same relation. No public page may exist as a straggler outside lawful organism/root topology.

## expression / translation

Semantic identity is independent of language.

- addresses and CCCC apertures do not translate;
- canonical INDEX nouns remain structural identity;
- visible concept labels, questions and prose are expression tissue and may have multiple language/modal realizations;
- switching language must preserve organism, address, topology, root boundary and current navigation state;
- current expressions are German and English;
- future languages or nonhuman communicative modalities may be added only when a truthful transduction exists. Absence remains absence rather than fabricated translation.

Root-level expression configuration lives in `encounter.json`; leaf expression tissue lives at `w/x/z/y/content.json`.

## lifecycle shell

- `_stomach/` — unresolved Philosophy-local nutrient.
- `_feed/` — current source-owned self-presentation when materialized/refreshed.
- `_root/` — append-oriented Philosophy HOME history.
- `_waste/` — retired Philosophy-local material.

These roles are Philosophy-local and do not merge with Display or cambium shells.

## current pressure

The next rank-invariance test is intentionally open: admit one already-real project/organism only after Philosophy's current four loci determine its truthful outer placement. Do not reverse-engineer the Philosophy split to accommodate a preferred project.

When an independently rooted project is admitted, preserve the form `philosophy:q ⟦ project:root ⟧`; entering it restarts semantic address space while Display keeps the same perceptual contract.

Compression: **one whole, four earned apertures, every realized place directly encounterable, expression freely translatable without translating identity.**
