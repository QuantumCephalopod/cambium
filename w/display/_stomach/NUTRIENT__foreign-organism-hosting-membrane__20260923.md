# NUTRIENT — foreign organism hosting membrane — 2026-09-23

status: OPEN / ARCHITECTURE PRESSURE
kind: display structural encounter
target: github.cambium → display

## source-faithful pressure

Philipp proposes making the Typography/Pretext hosting pattern recursively stable for future external organisms whose internal structure is not tetrahedral.

Working intuition:
- preserve an external organism/library/codebase as itself rather than rewriting its interior into w/x/z/y;
- grow a tetrahedral Display-owned membrane around the relation by which the foreign body is hosted;
- use the existing lifecycle shell (_stomach/_feed/_root/_waste) only where an independently rooted hosted whole actually earns that morphology;
- choose host scope by function: Pretext belongs under Display because it concerns how text becomes perceptible, not cambium root merely because it is shared;
- seek a rank-invariant pattern reusable for future foreign organisms.

Open distinction:
When is a foreign dependency merely a provider-constrained aperture/carrier, when is it a structural organ with its own root, and when does it deserve the full independently rooted lifecycle shell?


## correction — 2026-09-23

The first proposed realization over-differentiated the pressure by inventing a new Typography container and immediately assigning it a 1→4 split.

Current bounded correction:
- no new Typography organism/container is earned yet;
- Display already has an unsplit `w · Embodiment` locus, and Pretext is currently truthful Embodiment tissue;
- an intact foreign Pretext body may therefore occupy Display `w` without implying a new semantic split;
- future pressure may later split Embodiment; if that happens, Pretext may move to the descendant locus that then truthfully owns its function;
- consumers must therefore bind to stable foreign identity/capability rather than hard-coded semantic address;
- the existing vendored Pretext `VERSION.json` already witnesses stable package identity (`@chenglou/pretext`), version and source and is sufficient for this first case;
- do not generalize a new foreign-body manifest/schema until a second real case proves the native identity witness insufficient.

Open implementation question:
How should Display build/runtime resolve a stable foreign identity through current Display anatomy while stopping at independent organ/site-holon membranes, so relocation after later Cambium growth requires no consumer edits?
