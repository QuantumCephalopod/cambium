# STAGING BROWSER WITNESS — Global Navigation Aperture

**state:** STAGING BROWSER WITNESSED / USER + PRODUCTION ACCEPTANCE PENDING  
**date:** 2026-09-15  
**scope:** `w/display` persistent two-specimen membrane  
**authority:** empirical staging evidence only; not HOME, not production closure

## Exact artifact exercised

The browser witness exercised the exact GitHub Actions `staging-display` artifact generated from staging head `af3ab09a5ee7c76bda1bb3e95d2877deb77917cf` after all runtime/aperture changes. Later commits through `cb12d373135cd1cf6b57f825c1fb70e2c32a4783` changed only canonical Display RITUALS; a byte-for-byte directory comparison of the later Actions artifact against the witnessed artifact showed no generated membrane differences.

The artifact was loaded into system Chromium with all generated CSS/JS bytes inlined because this execution environment blocks direct local/network browser navigation. The browser itself still executed the real DOM, Pointer Events, timers, canvas/WebGL code, composition CSS, history state and runtime code from the generated artifact.

## Browser contact witnessed

- single encounter starts with Global Navigation Aperture closed and only the persistent membrane marker visible;
- pointer hover over the single-interlocutor outer-membrane marker opens the navigator pocket;
- click pins the aperture open and moving the pointer away leaves it open;
- Escape closes the aperture without leaving/changing the current interlocutor;
- x-axis was dragged to an arbitrary non-extreme value of approximately `31%` and held still;
- after the settle interval, `data-latched=true` while the value remained `31`;
- moving the still-held pointer far across the rail after latch left the value at `31`;
- pointer-up after latch left the value at `31`;
- picking the latched knob up and releasing before another settle returned the value to `0`;
- Philosophy and Papers were remounted together at one quotient locus to create a real split encounter;
- split composition produced two full local interlocutor backgrounds, each half-width and full-height;
- the same Global Navigation Aperture moved to the exact viewport seam center;
- hovering the seam marker opened one symmetric diamond navigator pocket;
- changing global orientation changed one shared `SSSWorldView.orientation` while both local backgrounds remained visible, confirming one global orientation state driving the split encounter;
- no browser page errors or console errors occurred during the witness.

## What this does not claim

This does not claim production acceptance, final aesthetics, final touch ergonomics, final aperture dimensions, or user acceptance. It only establishes that the staging architecture now materially embodies the derived invariants well enough for direct user contact before merge.

## Current pressure

Expose the staging result to the user, tune by direct use if needed, and only then decide whether the two-specimen membrane has earned promotion.
