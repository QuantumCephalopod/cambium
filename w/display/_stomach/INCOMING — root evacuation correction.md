# INCOMING — Display root evacuation correction

## Pressure

The Display 1→4 differentiation is semantically closed but physically incomplete. Active functional tissue still remains at `w/display/` root, and the host vertex carries an ad-hoc `w/interface.md` file that is not a rank-invariant organism primitive.

This violates the same whole-body redistribution law used by the original cambium split: after a realized 1→4 differentiation, active tissue is carried into the four children. The organism root remains only the canonical organism shell/receptors/constitution and its four child addresses.

## Required correction

1. `w/interface.md` ceases to exist as a special membrane concept. The membrane relation `cambium:w ⟦ display:ε ⟧` is structural law expressed by anatomy/RITUALS, not a one-off file type.
2. `w/display/` must contain only:
   - `INDEX.yaml`
   - `_cambium.yaml`
   - `RITUALS/`
   - `_feed/`
   - `_root/`
   - `_stomach/`
   - `_waste/`
   - `w/`, `x/`, `z/`, `y/`
3. Every active root file/directory is redistributed by the already-earned phenotype:
   - `w · Embodiment` — presentation/template/visual field/shader tissue;
   - `x · Continuity` — persistent runtime identity/state/activity/live-continuity tissue;
   - `z · Orientation` — navigation/address-view/camera/aperture/fold tissue;
   - `y · Population` — site-space bodies and population discovery/mount truth.
4. Root residue that is no longer build/runtime authority is retired to Display `_waste`, not kept as a fifth implicit organ.
5. Child `ROLE.md` marker files are not canonical primitives; phenotype meaning remains in `INDEX.yaml` / `_cambium.yaml` / RITUALS and the actual child tissue.
6. Build/check/workflow paths are rewired without changing the public encounter behavior.

## Acceptance

- `w/interface.md` is absent.
- Display root contains exactly the canonical shell/constitution/receptors plus `w/x/z/y`.
- No active build/runtime dependency points to former Display-root tissue.
- exact generated artifact passes existing tree/address/fold checks.
- real browser direct PHILOSOPHY ↔ PAPERS navigation remains unchanged.
- correction HOME is appended only after those witnesses; feed refresh remains downstream.

Compression: **finish the split physically: no special interface file, no fifth shared root layer, and no active tissue left at Display root.**
