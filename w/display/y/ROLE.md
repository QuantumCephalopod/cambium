# Population

Display-local CULTIVATE vertex and membrane into global site-space.

Crossing `display:y` restarts page-organism address space at `ε`:

`display:y ⟦ site-space:ε ⟧`

A direct child holon of `w/display/y/` occupies site-space overview. Address folders use the absolute Display address with the outer `y` custody prefix; stripping that first `y` yields the global site-space address. Thus `w/display/y/yy/` is site-space `y`, `w/display/y/yz/` is site-space `z`, and recursion continues identically.

Only address folders that physically contain a site-holon are admitted as global navigator targets. Empty potential geometry is not fabricated population.
