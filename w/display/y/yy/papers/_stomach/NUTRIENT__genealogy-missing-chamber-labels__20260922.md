# NUTRIENT — Papers genealogy morphology + missing chamber occupants + chamber labels — 2026-09-22

status: OPEN / UNRESOLVED
kind: display.papers browser witness encounter
target: github.cambium → display.papers
source: user browser screenshots + explicit observations in chat

## source-faithful encounter

The user reports three concrete browser symptoms on the current Papers staging body:

1. A selected `5H.8P2A` Holon appears to have one child rendered like a single Source tetrahedron. Descending into that child reaches a `4H` Holon that still looks Source-like rather than like a recursive Holon body.
2. One complete root chamber/container still appears to render no occupants.
3. Root chamber/container names are not visible; the user requests labels because this makes spatial debugging and orientation much harder.

The screenshots show selected Holon inquiry surfaces and the current overview after the fourfold-rest-frame / Inquiry-environment act.

## unresolved questions

### Q1 — genealogy / morphology
Trace `5H.8P2A` and the implicated `4H` through the live public `holon_meta` and identity projection.

Determine:
- its exact four direct parents;
- each parent's actual rank and kind;
- whether same-rank parent law is violated in projected source truth;
- whether renderer recursion collapses a legitimate Holon to one tetrahedron because LOD stops before exposing parents;
- whether a rank/parser/parent-index bug causes a Holon to be treated like Source ground;
- whether missing parent identities or incomplete projected genealogy trigger terminal rendering.

Do not infer rank from appearance alone.

### Q2 — apparently empty root chamber
Trace all four current root chambers end-to-end:
`public membership → identityIndex → buildRecords → recordById → populationBodies/collectBody → screen projection`.

Determine which chamber the user perceives as empty and whether its organisms:
- are absent,
- are culled,
- are rendered below visibility,
- overlap another chamber,
- are outside the visible viewport,
- or are present but visually indistinguishable from absence.

The prior x/y overlap diagnosis is not sufficient if the current browser still shows a whole chamber with no occupants.

### Q3 — chamber names
Add/derive root chamber labels only after the current geometry is traced.

Labels must:
- come from current Papers phenotype/root truth;
- attach to actual projected chamber geometry, not hard-coded screen corners;
- follow shared orientation/chamber focus;
- remain secondary/non-interactive;
- not invent deeper semantic addresses.

## preserved constraints

- Drive /papers remains source authority.
- Papers is independently rooted and floats inside Philosophy Inquiry material-space.
- Do not reintroduce the complete Philosophy tetrahedral body inside Papers.
- Preserve Source/Holon identity, actual genealogy, S quantum scale, recursive rank law, deterministic packing and local chamber passage unless evidence specifically reopens one.
- Browser appearance is evidence of a rendering problem, not authority to rewrite source truth.
- Completed work must retire to _waste; only unresolved residue remains in _stomach.
