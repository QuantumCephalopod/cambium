# cambium

**self-similar-systems saar**  
an independent lab for artistic and scientific inquiry.

> we give questions form—and let those forms question us.

cambium is the living website organism, not a mirror of the entire research body.
its public skin can be a bounded, reproducible artifact while the source practice
continues to change. the first root differentiation is materialized and the
current files are staged through `QuantumCephalopod/cambium` for an upstream pull
request. no upstream merge, public launch or solved dense-carrier compass is asserted.

## enter the body

read [`SKILLS/START_HERE.md`](SKILLS/START_HERE.md), then read
[`INDEX.json`](INDEX.json) completely.

`INDEX.json` is the organism's semantic self-overview. it follows the same recursive
`w/x/z/y` address grammar as the body instead of flattening that body into a file
lookup table. every realized address carries the locally earned noun, the current
semantic whole needed to recognize that place, and the living tissue resident there.
when an address later differentiates, its four children simply recur beneath it.

at the root, the current `whole` is the rank-4 volume earned by the completed
4V/6E/4F/1T metabolism. that is what higher work inherits: metabolized meaning,
not a diary of the reasoning that produced it. the detailed first differentiation
has therefore left canonical anatomy; a retained copy lives in
[`_stomach/root-differentiation.md`](_stomach/root-differentiation.md) as unresolved
nutrient rather than normal re-entry ground.

| address | whole | actual tissue |
|---|---|---|
| w / create | expression | template, style, renderer, mark |
| x / copy | continuity | approved public wording and organ descriptions |
| z / control | orientation | address algebra, route admission, interaction |
| y / cultivate | renewal | builder, checks, geometry specimen |

each of these four limbs is currently unsplit. the files inside them are tissue of
that local whole, not unearned further semantic branches.

## try it

open `index.html` directly in a browser. it is a self-contained generated skin:
no CDN, remote font, framework, package install, analytics or application server
is needed. scripts only enhance the already-readable landing page. embedded source
JSON is a generated projection of the semantic organism and public wording contract.

one navigation surface shows the named places of the actual body. each breadcrumb
opens its complete raw prefix. the geometry is the spatial expression of the same
address structure, not a second menu or an addressing demo.

at the first split, the names are expression, continuity, orientation and renewal.
at a later realized split, every step takes its own locally earned noun and whole
from the recursively nested index. reciprocal raw paths may occupy one geometric
locus without erasing either genealogy; paired trails appear only when independently
realized witnesses also converge on the same semantic whole. terminal self-refinement
likewise keeps its raw witness even when the geometric point is unchanged.

`y/specimen.json` exercises deeper recursive and reciprocal paths with explicitly
synthetic names without publishing those descendants. the current interaction
correction is in [`y/interaction.md`](y/interaction.md).

drag the spatial view horizontally or vertically to rotate it. dragging around
the rim permits roll. with the map focused, arrow keys turn and tilt, q/e roll,
and home resets. the reset-view button restores the starting camera without
moving to another address. tab and enter also navigate named places.

exact symbolic address keys and BigInt dyadic coordinates precede approximate
screen projection. terminal self repetition and the justified terminal sibling
junction may share geometry, but geometric quotient is not permission to erase
raw semantic witnesses. arbitrary path reversal is not an address law.

the public `study` namespace is retired. no fixture is shipped in the production
data block, and no unearned descendants have been added. the algebra remains
unbounded in definition; route parsing has an explicit 4,096-letter browser safety
budget. neither that budget nor viewport precision changes geometric identity.

## change and check

Python 3 and Node.js are development/test tools, not website runtime dependencies.
From this repository root:

```sh
python3 y/build.py
python3 y/check.py
node y/test-address.cjs
python3 y/build.py --check
```

on Windows, `py` can replace `python3`. edit living source tissue and
`INDEX.json`; root `index.html` is generated.

Optional browser checks require Python Playwright and Chromium:

```sh
python3 y/browser-check.py --out ../cambium-review
```

The browser test injects the self-contained document into Chromium and checks
named navigation, recursive test paths, free camera interaction and responsive
layouts. Add `--http` to exercise a local server where browser policy permits it.
The injection run does not verify DNS or a public deployment.

For serving local source-file links as well as the generated skin:

```sh
python3 -m http.server 8000 --bind 127.0.0.1
```

then visit `http://127.0.0.1:8000/`.

## publication boundary

[`_stomach/launch.md`](_stomach/launch.md) holds unresolved launch work, including
the upstream merge and the later Pages/domain steps. no personal contact address,
legal notice, blanket license or publication list has been invented. the
administrative Gmail address is not a public contact by default.

The repository is intended to be public. `_stomach`, `_waste`, `SKILLS`, source
code and git history are public material, even when not linked from the page.
With direct Pages root publication, shell files may also be reachable by URL.
Underscores, noindex, .gitignore and deletion are not privacy controls. No private
research, recovery material, credentials or unpublished third-party carriers
belong here.

`CNAME` records the intended `sss.saarland` address. `.nojekyll` permits direct
static publication. neither proves Pages or DNS is configured. resolve the
launch queue before enabling the site; a commit is not a public launch.

No license has yet been selected. Public visibility alone is not a blanket reuse
grant for the code, text, images or future research objects.
