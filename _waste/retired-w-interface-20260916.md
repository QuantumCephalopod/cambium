# expression — display organ membrane

**host address:** `w / expression`  
**host occupant:** `display`  
**physical custody:** `w/display/`

This file is the host-side membrane by which the intact `display` organ occupies
cambium's `w / expression` locus. The folder beneath this locus is an organ root, not
a second host character:

`cambium:w ⟦ display:root ⟧`

is **not** `cambium:ww`.

## current relation

- `display` owns the visitor-facing encounter body and its own local shell, intake,
  output and future internal constitution;
- host `x / continuity` preserves the inheritance relation by which public meaning
  remains traceable while display changes;
- host `z / orientation` supplies lawful address/navigation interfaces across the
  membrane;
- host `y / renewal` builds, witnesses and secretes the Pages membrane without
  becoming display's inner anatomy.

The host may therefore operate on display as one expression occupant while display
remains independently re-enterable at its own root. Internal display growth does not
mint host descendants, and movement or repair of host expression must not flatten the
organ's inner address space.

## structural restraint

`w` is currently an unsplit host vertex. `w/display/` is containment of one organ,
not a realized `ww` branch. Do not create `w/_cambium.yaml` or host descendants unless
new host-expression pressure independently earns a complete local CCCC split.
