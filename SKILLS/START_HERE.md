# start here — cambium

you are entering the website organism of self-similar-systems saar. its first
root split is locally materialized: w expression, x continuity, z orientation,
y renewal. those are this website's answers, not the wider practice's four nouns.

## re-entry

1. read this receptor completely.
2. read `../INDEX.json` (or its generated `../INDEX.md` presentation) for the
   actual body, stable occupant IDs, shell and unresolved frontier.
3. read `../ROOT_SPLIT.md` when root anatomy or placement is involved.
4. read the exact source files at the addressed locus before changing them.
5. reacquire only the relevant live shared procedure through the references below.
6. after changing living tissue, backpropagate the atlas, regenerate the skin,
   run checks and inspect the rendered result. remote persistence is separate.

## shared physiology — references rather than copies

- global receptor: https://drive.google.com/file/d/1gaJF8r9Rz8eCttAqOQ_8xq3Fd3SxUkOW/view
- dive-in: https://drive.google.com/file/d/1Vpat1Z61a8bPu3a-isRzsifPd6iAjRHo/view
- metabolism: https://drive.google.com/file/d/1yyIOqzzYnEbkK-gpAecfK3X6BtbEAmAr/view
- cambium: https://drive.google.com/file/d/1FVfEJN9MWEzRg3JF_dpCGwWyq92SvmyA/view

the references do not grant access. they are procedural authority, not website
runtime dependencies. do not reconstruct unavailable current versions from memory.
Git writes use the GitHub connector's exact contracts; Drive-specific mutation
skills apply only when an operation actually changes Drive.

## local contract

- `w=CREATE · x=COPY · z=CONTROL · y=CULTIVATE` is fixed DNA.
- the root has become navigation wood. active source content resides at w/x/z/y.
- root `index.html` is a generated outward skin, not another authored whole.
- root `INDEX.md` is a generated readable atlas; `INDEX.json` is the live atlas.
- four leaf directories are carrier containers for the four unsplit wholes.
  they do not imply deeper splits.
- the visible interface is lowercase; protocol and source-filename casing stays exact.
- every actual semantic node needs admitted anatomy and a receipt. a picture,
  fixture or convenient file directory is not enough.
- `y/specimen.json` is a test-only named geometry fixture. it is not embedded in
  the public skin and never contributes descendants to the real website atlas.
- there is one public atlas and one navigation landmark. every visible step and
  map label uses the actual full-prefix noun, never a generic letter label.
- preserve symbolic paths beneath mutable display names. camera yaw, pitch and
  roll only change the projection. pointer gestures must not trigger route clicks.
- `y/interaction.md` records the current scoped usability refinement; it does not
  supersede the first root split or claim further semantic differentiation.
- the two address trails describe available approaches, not remembered history.
  prefix links must target the complete prefix. shared location preserves both
  chambers. only actual witnesses may appear in the real site's trail.
- geometry is exact before display. no arbitrary full-string reversal, nearest
  pixel identity, silent chamber merging or fabricated navigation depth.
- the full dense Tetrix compass and its unresolved membership classifier are
  not implemented by this finite known-address navigator.
- a released research object is a bounded public surface; source research may
  continue. still-cited versions are not disposable simply because they are older.

## intake and witness

only publication-safe website nutrients enter `_stomach`. unresolved items need
an exit condition. `_waste` is spent residue, not an archival justification for
keeping dead scaffolding in the current body.

run `python3 y/build.py`, `python3 y/check.py`, `node y/test-address.cjs` and,
when available, `python3 y/browser-check.py`. these are bounded checks, not a
full accessibility/security/legal audit. a human must review publication and
interpretation choices.

before remote writes reacquire the repository and preserve other changes. a
reported permission flag is not evidence that a write succeeded. stop on a 403;
do not evade the integration boundary with a different transport. claim a commit
only after provider confirmation and readback. Pages, DNS, app access, personal
contact publication and license selection require their own authorization.
