---
name: cambium
description: "Local ritual receptor for the cambium website organism: preserve its host phenotype, nested display boundary, lifecycle shell, publication membrane, and substrate-invariant HOME/_feed dance while leaving GitHub-specific mechanics beneath organism law."
version: "1.0"
---

# CAMBIUM ORGANISM RITUAL — local root

`cambium` is the living website organism of self-similar-systems saar. This repository is one persistent carrier of that whole, not a competing ontology.

## authority and re-entry

- Enter shared root law through the canonical My Drive `RITUALS/` receptor before interpreting this organism.
- Then enter this repository root itself and read the relevant local `RITUALS/` procedure. There is no `START_HERE`, `SKILLS`, Diamond compatibility layer, or repository-specific boot doctrine between the whole and its receptor.
- `QuantumCephalopod/cambium` is the writable staging carrier. `self-similar-systems/cambium` becomes organization truth only by deliberate owner promotion/merge and provider witness.
- Git history, Actions, Pages, commits and workflow logs are substrate witnesses/mechanics. They are not semantic body, `_root`, `_feed`, or ritual authority.

## current host phenotype

Root `INDEX.yaml` is the minimal recursive phenotype:
- `w · expression`
- `x · continuity`
- `z · orientation`
- `y · renewal`

Root `_cambium.yaml` is the closed root `4V/6E/4F/1T` and nothing else. Raw `w/x/z/y` folders are stable carrier addresses; mutable semantic nouns live in INDEX.

The current root split is host physiology. It is not automatically visitor-facing taxonomy.

## lifecycle shell

This independently rooted whole exposes the same lifecycle roles as every other organism:

- `_stomach/` — unresolved admitted nutrient.
- `_feed/` — source-owned current self-presentation. `_feed/current.json` is a derived view and never internal authority.
- `_root/` — append-oriented local HOME history. Each HOME is an immutable event object; ordinary re-entry does not scan this history.
- `_waste/` — retired/spent matter whose local function has closed.

Canonical local motion remains:

`act → witness → durable HOME → asynchronous _feed refresh → return/circulation`

HOME closes when the source outcome and its local `_root` event are durable. `_feed` catch-up is a separate mechanical consequence and must expose which HOME it reflects. GitHub Actions may actuate that relation, but Actions do not decide HOME validity, semantic meaning, admission, care, phenotype, or routing.

## display organ

`w/display/` is one intact independently re-enterable organ participating at host `w / expression`.

Boundary:

`cambium:w ⟦ display:root ⟧`

Physical custody is not host `ww`. Crossing the membrane restarts address space at display root.

Display remains internally unsplit: `w/display/INDEX.yaml == {}` and no `w/display/_cambium.yaml` exists. Its future differentiation must be earned from display pressure.

Host continuity, orientation and renewal remain host interfaces consumed across the membrane; they do not become display descendants.

## publication membrane

`_site/` is an ephemeral secreted Pages artifact, never living anatomy and never `_feed`.

The mechanical build/witness path remains:

```sh
python3 y/build.py --artifact _site
python3 y/check.py --artifact _site
SITE_DIR=_site node y/test-address.cjs
python3 y/build.py --artifact _site --check
```

`.github/workflows/pages.yml` is substrate circulation only. It may mechanically project `_feed` when new durable HOME pressure appears, then build/witness the public membrane. Only the canonical organization repository may deploy Pages.

## current living pressure

- Root `_stomach/launch.md` remains unresolved launch pressure.
- Completed derivation residue in `_stomach` is not automatically current anatomy and may later be retired under metabolism law.
- Display-local visitor encounter, care, becoming and navigation nutrients remain owned by `w/display/_stomach/`.
- Do not force display to expose host `expression / continuity / orientation / renewal` as visitor ontology merely because those host organs are real.

## closure

A cambium mutation closes only when:
1. live root/shared ritual law has earned the change;
2. the smallest affected local dependency cone is mutated;
3. current source state is witnessed;
4. durable local HOME is appended in `_root`;
5. any `_feed` catch-up remains a separate mechanical consequence;
6. build/address witnesses pass when the public membrane is affected.

Compression: **same organism dance, same discoverable shell, different substrate mechanics.**
